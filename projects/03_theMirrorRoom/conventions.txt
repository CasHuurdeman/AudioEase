fields in a Class: m_field, locale variabelen binnen een functie niet

Class met hoofdletter en constructors ook
de rest begint met kleine letters, ook objecten

Everything that can fit on one line
--> HEADER ONLY

Haakjes:

void foo(int bar)
{
    bar += 4;
    return bar;
}

int getFoo() { return foo; }