//
// Created by cashu on 15/12/2025.
//

#pragma once

class Smoothe {
public:
  // Smoothe() = default;
  // ~Smoothe() = default;

    static float smootheValue(float value, float targetValue, int numSteps);

    // void setTarget(float value, float targetValue, int numSteps);

private:
    // float m_value = 0;
    // float m_targetValue = 0;
    // float m_stepSize = 0;
    // bool posDelta = true;
};
